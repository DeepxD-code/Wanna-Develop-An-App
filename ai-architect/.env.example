# ============================================================
# AI ARCHITECT — FRONTEND CONFIG
# API keys are managed by the BACKEND (application.yml)
# Frontend only needs the backend URL.
# ============================================================

VITE_BACKEND_URL=http://localhost:8080/api
VITE_PHASE_DELAY_MS=2000
VITE_MAX_RETRIES=3
